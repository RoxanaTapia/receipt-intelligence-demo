.env
.env.local
.env.*.local

# Runtime receipt JSON (keep directory via .gitkeep)
data/receipts/*
!data/receipts/.gitkeep

# Caddy edge secrets / generated TLS (see deploy/generate-*.sh)
deploy/caddy-basicauth.conf
deploy/certs/

.DS_Store
